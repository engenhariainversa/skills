# Qualquer host desconhecido: responde 444 (fecha a conexao) em vez de entregar o primeiro site.
server {
    listen 80 default_server;
    server_name _;
    location /.well-known/acme-challenge/ { root /var/www/certbot; }
    location / { return 444; }
}
